{
  "name": "parse-server-jobs-scheduler",
  "version": "1.1.1",
  "description": "Jobs scheduler for Parse Server",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rm -rf ./dist/* && npx tsc",
    "prepare": "rm -rf ./dist/* && npx tsc"
  },
  "author": "lcpmarvel",
  "license": "ISC",
  "devDependencies": {
    "@types/cron": "^1.7.3",
    "@types/moment": "^2.13.0",
    "@types/node": "^16.11.1",
    "@types/parse": "^2.18.13",
    "typescript": "^4.4.4"
  },
  "peerDependencies": {
    "parse": "^3.2.0"
  },
  "dependencies": {
    "axios": "^0.23.0",
    "cron": "^1.8.2",
    "moment": "^2.29.1"
  }
}
